# pasos para el flujo basico de git 
```sh
git status 
git add, o git add -a
git status
git comit -m "descripcion del cambio"
git push
```

## Nota: 
el mensaje de commit debe de ser una oracion descriptiva del cambio 